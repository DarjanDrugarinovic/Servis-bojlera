import { regularBoilers } from "./regular-boilers";
import { flowBoilers } from "./flow-boilers";
import { services } from "./services";

export const assets = {
  regularBoilers,
  flowBoilers,
  services,
};
